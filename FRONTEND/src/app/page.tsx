import Homepage from "@/components/Base";
import React from "react";

type Props = {};

export default function page({}: Props) {
  return (
    <div>
      <Homepage children={undefined} />
    </div>
  );
}
